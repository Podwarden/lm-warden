## What this changes

<!-- One or two sentences, and the issue it closes if there is one: Closes #123 -->

## How it was tested

<!-- Commands you ran and what they showed. -->

- [ ] `make lint`, `make typecheck` and `make test` pass
- [ ] `npm test` and `npm run typecheck` in `frontend/` pass (if the UI changed)
- [ ] `CHANGELOG.md` has a line under `[Unreleased]` for a user-visible change
- [ ] Docs in `documents/` or the README updated if behaviour changed
- [ ] No engine patching: a launch is still argv plus environment
